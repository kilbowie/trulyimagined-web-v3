# ============================================
# Truly Imagined Web App - Environment Variables
# ============================================
# Copy this file to .env.local for TI web development.
# Ownership matters: TI app vars and HDICR client vars belong here;
# HDICR internal secrets do not.

# ============================================
# TRULY IMAGINED - Application Runtime
# These values belong to the TI web app itself.
# ============================================

# Auth0 (TI application login)
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your_client_id_here
AUTH0_CLIENT_SECRET=your_client_secret_here
AUTH0_ISSUER_BASE_URL=https://your-tenant.auth0.com
AUTH0_SECRET=use_a_long_random_value_at_least_32_characters
AUTH0_BASE_URL=http://localhost:3000
AUTH0_AUDIENCE=https://api.trulyimagined.com
AUTH0_ROLES_CLAIM_NAMESPACE=https://trulyimagined.com/roles

# Public app configuration
NEXT_PUBLIC_APP_NAME=Truly Imagined
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3001
NODE_ENV=development

# TI-owned database access (temporary until full Phase 4 separation)
# Use only for TI-owned tables and routes; do not point this at HDICR-only secrets.
# Preferred single-URL setting for TI runtime (used by apps/web/src/lib/db.ts)
TI_DATABASE_URL=postgresql://postgres:password@localhost:5432/trulyimagined
# Legacy component settings still used by selected scripts/routes
DATABASE_HOST=your-rds-endpoint.region.rds.amazonaws.com
DATABASE_NAME=trulyimagined
DATABASE_USER=postgres
DATABASE_PASSWORD=your_password_here
DATABASE_PORT=5432
DATABASE_SSL=true

# TI media and payments integrations
AWS_REGION=eu-west-2
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_S3_BUCKET_NAME=trimg-actor-media
USE_MOCK_S3=true

STRIPE_SECRET_KEY=sk_test_your_secret_key_here
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Optional legacy KYC providers
ONFIDO_API_TOKEN=test_your_onfido_api_token_here
YOTI_CLIENT_SDK_ID=your_yoti_sdk_id_here

# ============================================
# HDICR - Platform API Client Config
# TI's credentials to call HDICR APIs. These are client credentials,
# not HDICR internal service secrets.
# ============================================

# Modes: local | remote
# local: use in-repo adapter implementations (dev/test only)
# remote: call HDICR service APIs (required for production)
HDICR_ADAPTER_MODE=remote
# Optional per-domain overrides (only set if intentionally different from global)
# HDICR_IDENTITY_ADAPTER_MODE=remote
# HDICR_CREDENTIALS_ADAPTER_MODE=remote
HDICR_REMOTE_BASE_URL=http://localhost:4001
HDICR_CLIENT_ID=your_hdicr_m2m_client_id
HDICR_CLIENT_SECRET=your_hdicr_m2m_client_secret

# ============================================
# HDICR Internal Secrets - NOT for TI Web
# Removal target from TI config surface: 2026-06-30
# These must live only in HDICR service deployment config / AWS secrets.
# ============================================

# DATABASE_URL=postgresql://user:pass@host:5432/hdicr  # Legacy compatibility only; prefer TI_DATABASE_URL
# ISSUER_ED25519_PRIVATE_KEY=base64_or_pem_value_here
# CONSENT_SIGNING_PRIVATE_KEY=base64_or_pem_value_here
# ENCRYPTION_KEY=your_64_character_hex_key_here
